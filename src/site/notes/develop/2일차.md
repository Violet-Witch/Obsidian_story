---
{"dg-publish":true,"permalink":"/develop/2/"}
---

# 『경주 돌이면 옥돌인가? 일단 여긴 서울이고.』
> 오늘의 교훈: *"예쁜 게 좋으면 눈으로만 보자."*
 
 
***
### [개발 목표 : Vertra 웹사이트 제작하기]
- [ ] ~~웹사이트 테마에 Css 스니펫 적용~~(실패!)
- [ ] 피그마로 웹사이트 디자인 구축하기
- [ ] Vertra 메인페이지 만들기
- [ ] 하위 페이지 상속 & 커뮤니티 탭 간단히

### [사용한 개발 툴]
- **python** - 프로그래밍 언어. 제일 쉬워서 했습니다.
- **Django** - 파이썬 기반 웹 프레임워크입니다. 
- **VSCode** - 원래 아톰 썼는데 힙스터병이 치료되었습니다.
- **figma** - 디자인 툴입니다. 제가 한 게 없어서 설명할 게 없음.
 
 
***
## [0. 웹사이트 테마에 CSS 스니펫 적용]

### CSS 스니펫💡
- 옵시디언 테마에 CSS를 사용자가 적용할 수 있게 해주는 조각입니다.
- 어차피 옵시디언 저밖에 안 쓰니까 적용 방법은 설명 안 해도 되겠죠.
- [링크는 달아주겠다]([옵시디언 기초: CSS Snippets 적용하기](https://kaminik.tistory.com/entry/%EC%BB%A4%EC%8A%A4%ED%85%80-CSS-CSS-snippets-%EC%A0%81%EC%9A%A9%ED%95%98%EA%B8%B0))

### 왜 너도 내가 우스워❓
![Pasted image 20260210223404.jpg](/img/user/develop/Pasted%20image%2020260210223404.jpg)
- 네. 제 웹사이트는 별도 CSS를 사용해서 적용이 안 됩니다.
- 제가 디자이너도 아니고 이거에 시간을 오래 쓸 순 없어서 깔끔하게 포기하기로 했습니다.
- 미리 아무거나 하나 적용해서 확인해보길 천만다행.
- ~~개발자가 웹사이트랑 개인노트 연동 문제를 고려 안 했다고?? / 왜 너도 내가 우스워?~~

### However❕
- 피그마는 잘 만들었습니다.
- 대략적인 웹사이트(Vertra) 구상은 끝냈으니 걱정 안 하셔도 됩니다.
- 어차피 우리 목적은 개발일지 꾸미기가 아닙니다.

### But❗
- 디자인 제가 한 거 아닙니다.
- 근데 만드는 건 내가 해야됨.
- 죽을래.

### Nevertheless‼️
- 아무 관련도 없지만 이 사이트 양식은 대충 정해졌습니다.
- 영원히 목차 바로가기 안 만들어줄거임.


> [!abstract] 여담
> - [요즘 꽂힌 노래🎶](https://www.youtube.com/watch?v=t7hmovsG_f0&list=RDJL3yuiNtR1k&index=3)
> - 나 집에 갈래.
> - 춥고 배고파요.
> - ![Pasted image 20260210225641.png](/img/user/develop/Pasted%20image%2020260210225641.png)



---
## [1. 메뉴 바 만들기]

#### [HTML 구조 변경]
`base.html`을 다음과 같이 수정합니다.
```html
{% load static %}
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}VERTRA{% endblock %}</title>
    
    <link rel="stylesheet" href="{% static 'base/style.css' %}"> <!--css불러오기-->
    <link rel="stylesheet" as="style" crossorigin href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.css" />
</head>

<body>
    <header class="main-header">
        <div class="header-container">
            <h1 class="logo">
                <a href="/">VERTRA</a><!--회사명-->
            </h1>
            
            <nav class="gnb"> <ul><!--메뉴 목록-->
                    <li><a href="/" class="active">홈</a></li>
                    <li><a href="/about">회사 소개</a></li>
                    <li><a href="/defense">Defense & Space</a></li>
                    <li><a href="/business">사업 분야</a></li>
                    <li><a href="/security">보안</a></li>
                    <li><a href="/news">최신 뉴스</a></li>
                    <li><a href="/community">커뮤니티</a></li>
                    <li><a href="/careers">채용</a></li>
                    <li><a href="/contact">연락처</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        {% block content %}
        {% endblock %}
    </main>

    <footer class="main-footer">
        <div class="footer-container">
            <p>Copyright © 2026 VERTRA. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>
```

#### [CSS 구조 변경]
`style.css`를 다음과 같이 변경합니다.
```css
/* 기본 초기화 */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Pretendard', sans-serif;
    background-color: rgb(10, 10, 10);
}

/* 네비게이션 바 컨테이너 (HTML의 .main-header와 일치) */
.main-header {
    width: 100%;
    padding: 20px 0;
    background-color: rgba(15, 20, 50, 0.5);
    position: fixed;
    top: 0;
    z-index: 1000;
    border-bottom: 1px solid rgba(0, 0, 0, 0.9);
}

/* 내부 컨테이너 (HTML의 .header-container와 일치) */
.header-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 40px;
}

/* 로고 스타일 */
.logo a {
    color: #ffffff;
    font-size: 24px;
    font-weight: 800;
    text-decoration: none;
    letter-spacing: 1px;
}

/* 메뉴 리스트 스타일 (HTML의 .gnb와 일치) */
.gnb ul {
    list-style: none;
    display: flex;
    gap: 30px;
}

.gnb ul li a {
    color: #a0a0a0;
    text-decoration: none;
    font-size: 15px;
    font-weight: 500;
    transition: color 0.3s ease;
}

/* 호버 효과 및 활성화 상태 */
.gnb ul li a:hover,
.gnb ul li a.active {
    color: rgb(218, 13, 64); /* 포인트컬러 */
}
```

#### [고마워요 스피드웨건!: 왜 이렇게 만들었는가?]
**적용된 메뉴 화면**
![Pasted image 20260211012135.png](/img/user/develop/Pasted%20image%2020260211012135.png)
기본적으로 10:30:60 색조합을 따랐습니다
기본 글씨는 일단 보여야되니까 대충 흰색~회색 사이에서 고른듯

포인트컬러(10)
- rgb(218, 13, 64)
- 별 이유는 없고 백예훈 눈 색입니다

배경색1(30)
- rgba(15, 20, 50, 0.5)
- 남색입니다.
- 네비게이션 바 색.

배경색2(60)
- rgb(10, 10, 10)
- 바탕 검정색입니다.
- 방산업체 분위기가 밝은것도 웃기잖아.

>[!warning] 주의
>- CSS 적용 안 됨 문제가 생기면 브라우저에서 `Ctrl` + `Shift` + `R`
>- 아마 화면 왼쪽 상단에 저번에 썼던 글씨가 보일텐데 정상입니다.
>- 이 다음에 바로 고칠거임.

>[!tip] Tip
>- 메뉴가 많죠? 원래 이렇게 만들면 안 됩니다.
>- 근데 이렇게 안 하면 회사 임직원용 커뮤니티 따로 만들어야됨.
>- 싫어요.
>- 이것도 빡세 이미.
>- 개발비 지원 받습니다.

---
### [2. 홈 화면 만들기]

#### [HTML 구조 변경]
`home.html`을 수정합니다.
```html
{% extends 'base/base.html' %}
{% load static %}

{% block title %}VERTRA - 정상을 향한 여정{% endblock %}

{% block content %}
<section class="hero-section">
    <div class="hero-container">
        <div class="hero-text">
            <h1>
                정상을 향한 여정<br>
                <span class="highlight">VERTRA</span>
            </h1>
            <p>
                Vertex와 Astra의 결합, 우주와 방위의 정상까지 오르겠다는 비전으로<br>
                대한민국의 안보와 우주 산업을 선도합니다.
            </p>
            <div class="cta-group">
                <a href="/defense" class="btn btn-primary">시작하기 &rarr;</a>
                <a href="/about" class="btn btn-outline">자세히 보기</a>
            </div>
        </div>

        <div class="identity-card">
            <div class="card-item">
                <div class="icon-circle">⛰️</div>
                <div class="item-text">
                    <h3>Vertex, 정점을 향해</h3>
                    <p>짧은 시간 동안 쌓아올린 압도적인 기업 성과</p>
                </div>
            </div>
            
            <div class="divider"></div>

            <div class="card-item">
                <div class="icon-circle">⭐</div>
                <div class="item-text">
                    <h3>Astra, 가장 높은 곳까지</h3>
                    <p>미래를 결정짓는 차세대 우주 기술과의 연계</p>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
```

#### [CSS 구조 변경]
`style.css`을 수정합니다.
```css
/* 기본 초기화 */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Pretendard', sans-serif;
    background-color: rgb(10, 10, 10);
}

/* 네비게이션 바 컨테이너 (HTML의 .main-header와 일치) */
.main-header {
    width: 100%;
    padding: 20px 0;
    background-color: rgba(15, 20, 50, 0.5);
    position: fixed;
    top: 0;
    z-index: 1000;
    border-bottom: 1px solid rgba(0, 0, 0, 0.9);
}

/* 내부 컨테이너 (HTML의 .header-container와 일치) */
.header-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 40px;
}

/* 로고 스타일 */
.logo a {
    color: #ffffff;
    font-size: 24px;
    font-weight: 800;
    text-decoration: none;
    letter-spacing: 1px;
}

/* 메뉴 리스트 스타일 (HTML의 .gnb와 일치) */
.gnb ul {
    list-style: none;
    display: flex;
    gap: 30px;
}

.gnb ul li a {
    color: #a0a0a0;
    text-decoration: none;
    font-size: 15px;
    font-weight: 500;
    transition: color 0.3s ease;
}

/* 호버 효과 및 활성화 상태 */
.gnb ul li a:hover,
.gnb ul li a.active {
    color: rgb(218, 13, 64); /* 포인트컬러 */
}



/* 이 아래로 추가된 코드 */



/* 히어로 섹션 전체 레이아웃 */
.hero-section {
    width: 100%;
    height: 100vh;
    background-color: #000000; /* 배경 검정 고정 */
    display: flex;
    align-items: center;
    overflow: hidden;
}

.hero-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 40px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    gap: 50px;
}

/* 좌측 텍스트 스타일 */
.hero-text { flex: 1.2; color: #ffffff; }
.highlight { color: #da0d40; } /* VERTRA 레드 */

/* 1. 제목과 본문 사이의 간격 수정 */
.hero-text h1 {
    font-size: 56px;
    font-weight: 800;
    line-height: 1.2;
    margin-bottom: 30px;
}

/* 2. 본문 텍스트의 행간 및 하단 여백 수정 */
.hero-text p {
    font-size: 18px;
    color: #d1d1d1;
    line-height: 1.8;    /* 1.5에서 1.8로 늘려 문장 간 겹침 방지 */
    margin-bottom: 60px; /* 40px에서 60px로 늘려 버튼이 글자를 가리지 않게 함 */
}

/* 3. 버튼 그룹의 배치 확인 (선택 사항) */
.cta-group {
    display: flex;
    gap: 15px;
    margin-top: 10px;    /* 추가적인 안전장치로 상단 여백 부여 */
}

/* 우측 아이덴티티 카드 스타일 */
.identity-card {
    flex: 0.8;
    background: rgba(255, 255, 255, 0.03); /* 아주 투명한 흰색 박스 */
    border: 1px solid rgba(218, 13, 64, 0.3); /* 연한 레드 보더 */
    padding: 40px;
    border-radius: 12px;
    backdrop-filter: blur(10px); /* 배경 블러 처리로 세련미 추가 */
}

.card-item {
    display: flex;
    align-items: flex-start;
    gap: 20px;
}

.icon-circle {
    width: 50px;
    height: 50px;
    background: #1a1c23;
    border: 2px solid #da0d40;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    flex-shrink: 0;
}

.item-text h3 {
    color: #ffffff;
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 8px;
}

.item-text p {
    color: #a0a0a0;
    font-size: 14px;
    line-height: 1.5;
}

.divider {
    height: 1px;
    background: linear-gradient(to right, #da0d40, transparent);
    margin: 30px 0;
    opacity: 0.5;
}


.btn {
    display: inline-block;
    padding: 15px 32px;
    font-size: 16px;
    font-weight: 600;
    text-decoration: none;
    border-radius: 8px;
    
    /* 부드러운 애니메이션 효과 */
    transition: transform 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease;
}

/* 메인 레드 버튼 호버 효과 */
.btn-primary:hover {
    /* 기존 #da0d40보다 밝은 레드로 변경 */
    background-color: #ff1a53; 
    border-color: #ff1a53;
    
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(20, 25, 55, 1);
}

/* 외곽선 버튼 호버 효과 */
.btn-outline:hover {
    /* 배경을 반투명한 흰색으로 채워 밝아지는 효과 부여 */
    background-color: rgba(255, 255, 255, 0.15); 
    border-color: #ffffff;
    
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(20, 25, 55, 1);
}

/* 버튼 스타일 (기존 유지 및 미세 조정) */
.btn-primary {
    background-color: #da0d40;
    border: none;
    padding: 14px 28px;
    color: white;
    cursor: pointer;
    border-radius: 6px;
    text-decoration: none;
}

.btn-outline { border: 1px solid #ffffff;
    padding: 14px 28px; color:
    white; margin-left: 10px;
    border-radius: 6px; 
    text-decoration: none;
}
```

#### [메인페이지 결과물]
![Pasted image 20260211020238.png](/img/user/develop/Pasted%20image%2020260211020238.png)

>[!tip] Tip
>- 끝. 메인페이지 만든 것 치고는 뭐가 없죠?
>- 원래 여기까지만 하려고 했는데 시간이 4시간이 남음
>- 더 만들어야 한단 소리임
>- 젠장.


## [3. 페이지 연결시키기]
네비게이션 바에 실제 페이지 url을 연결시켜야 합니다.

#### [url 수정하기]
`base/urls.py`를 수정합니다.
```python
from django.urls import path
from . import views

app_name = 'base'

urlpatterns = [
    path('', views.home, name='home'),
    path('about/', views.about, name='about'),
    path('defense/', views.defense, name='defense'),
    path('business/', views.business, name='business'),
    path('security/', views.security, name='security'),
    path('news/', views.news, name='news'),
    path('community/', views.community, name='community'), # 방명록 기능 이전 예정 영역
    path('careers/', views.careers, name='careers'),
    path('contact/', views.contact, name='contact'),
]
```

`mysite/urls.py`를 수정합니다. 내용 다르니까 수정하세요.
```python
from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.write),
    path('', include('base.urls')), # base 앱의 URL 연결
]
```

'base.html'을 다음과 같이 수정합니다.
```html
<nav class="gnb">
    <ul>
        <li><a href="{% url 'base:home' %}" class="active">홈</a></li>
        <li><a href="{% url 'base:about' %}">회사 소개</a></li>
        <li><a href="{% url 'base:defense' %}">Defense & Space</a></li>
        <li><a href="{% url 'base:business' %}">사업 분야</a></li>
        <li><a href="{% url 'base:security' %}">보안</a></li>
        <li><a href="{% url 'base:news' %}">최신 뉴스</a></li>
        <li><a href="{% url 'base:community' %}">커뮤니티</a></li>
        <li><a href="{% url 'base:careers' %}">채용</a></li>
        <li><a href="{% url 'base:contact' %}">연락처</a></li>
    </ul>
</nav>
```

#### [내부 함수성 뷰 작성]
뭔가 작동은 해야겠죠? 예 그거 만들겁니다.
`base/views.py`를 수정합니다.
```python
from django.shortcuts import render
from .models import Post 

def home(request):
    """
    메인 히어로 페이지를 렌더링합니다.
    """
    return render(request, 'base/home.html')

def about(request):
    """
    회사 소개 페이지를 렌더링합니다.
    """
    return render(request, 'base/about.html')

def defense(request):
    """
    Defense & Space(무기 및 우주 기술) 페이지를 렌더링합니다.
    """
    return render(request, 'base/defense.html')

def business(request):
    """
    사업 분야 페이지를 렌더링합니다.
    """
    return render(request, 'base/business.html')

def security(request):
    """
    보안 페이지를 렌더링합니다.
    """
    return render(request, 'base/security.html')

def news(request):
    """
    최신 뉴스 페이지를 렌더링합니다.
    """
    return render(request, 'base/news.html')

def community(request):
    """
    방명록 기능이 이전된 커뮤니티 페이지입니다.
    DB에서 모든 게시물을 가져와 전달합니다.
    """
    posts = Post.objects.all().order_by('-created_at')
    return render(request, 'base/community.html', {'posts': posts})

def careers(request):
    """
    채용 정보 페이지를 렌더링합니다.
    """
    return render(request, 'base/careers.html')

def contact(request):
    """
    연락처 및 문의 페이지를 렌더링합니다.
    """
    return render(request, 'base/contact.html')
```

그리고 저기 해당하는 html 다 만들면 됨,
![Pasted image 20260211025514.png](/img/user/develop/Pasted%20image%2020260211025514.png)

>[!note] Wow.
>이제 오류는 안 날겁니다. 아마도. 근데 내용 하나도 없을거임. 아직 시간 3시간 남음. 난 어쩌다 이런 길에 들었는가. 


## [4. 네비게이션 바 적용 & 커뮤니티 탭 작성]

#### [네비게이션 바 적용]
만든 html 문서들 맨 위에 다음과 같은 코드를 적용합니다.
```html
{% extends 'base/base.html' %}
```
다 적용하셔야 합니다.

#### [커뮤니티 탭 작성]
대충 모양만 잡아두는 겁니다. 수정에 수정을 할 예정입니다.
`community.html`에 다음과 같은 코드를 적용합니다.
```html
{% extends 'base/base.html' %}

{% block title %}커뮤니티 - VERTRA{% endblock %}

{% block content %}
    <div class="community-container">
        <div class="page-header">
            <h1>커뮤니티</h1>
            <p>VERTRA 임직원용 공개 게시판입니다.</p>
        </div>

        <div class="post-list">
            {% for post in posts %}
                <div class="post-box">
                    <div class="post-header">
                        <h3>{{ post.title }}</h3>
                        <small>{{ post.created_at|date:"Y-m-d H:i" }}</small>
                    </div>
                    <div class="post-body">
                        <p>{{ post.content }}</p>
                    </div>
                </div>
            {% empty %}
                <div class="empty-state">
                    <p>아직 작성된 글이 없습니다. 첫 번째 글을 남겨보세요.</p>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
```

'style.css' 맨 하단에 이를 추가합니다.
```css
/* --- 커뮤니티 페이지 스타일 --- */

/* 1. 상단 여백 확보 (네비게이션 바에 가리지 않게 함) */
.community-container {
    max-width: 800px;
    margin: 0 auto;
    padding: 120px 20px 60px; /* 위 120px, 좌우 20px, 아래 60px */
    color: #ffffff;
}

/* 2. 페이지 제목 영역 */
.page-header {
    margin-bottom: 40px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1); /* 은은한 구분선 */
    padding-bottom: 20px;
}

.page-header h1 {
    font-size: 36px;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 10px;
}

.page-header h1::after {
    content: '.';
    color: #da0d40; /* VERTRA 포인트 레드 */
}

.page-header p {
    color: #a0a0a0;
    font-size: 16px;
}

/* 3. 게시글 박스 스타일 (카드 형태) */
.post-box {
    background-color: #1a1c23; /* 배경보다 약간 밝은 남색 */
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    padding: 25px;
    margin-bottom: 20px; /* 게시글 사이 간격 */
    transition: transform 0.2s ease, border-color 0.2s ease;
}

/* 게시글 마우스 올렸을 때 효과 */
.post-box:hover {
    transform: translateY(-3px);
    border-color: rgba(218, 13, 64, 0.5); /* 붉은색 테두리 발광 */
}

/* 게시글 제목과 날짜 */
.post-header {
    display: flex;
    justify-content: space-between; /* 제목과 날짜 양끝 정렬 */
    align-items: center;
    margin-bottom: 15px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    padding-bottom: 15px;
}

.post-header h3 {
    font-size: 20px;
    color: #ffffff;
    font-weight: 600;
}

.post-header small {
    color: #777;
    font-size: 14px;
}

/* 게시글 본문 */
.post-body p {
    color: #d1d1d1;
    line-height: 1.6;
    font-size: 16px;
}

/* 글이 없을 때 */
.empty-state {
    text-align: center;
    padding: 60px 0;
    color: #777;
    border: 1px dashed #333;
    border-radius: 12px;
}
```

---
## [5. 오늘도 해냈다.]

>[!todo] 다음 과제
>- [ ] 회사 소개 탭 만들기
>- [ ] 커뮤니티 탭 기능 모두 추가하기
>	1. 사용자 직업군 보이게
>	2. 댓글 작성 기능
>	3. 회원가입/로그인

>[!Abstract] 여담
>- 저번보다 한 게 많아보일지 모르겠지만 이번이 훨씬 쉽습니다.
>- 그리고 저번엔 이거 디스코드랑 연결하고 웹사이트 게시하고 하여간 보통 일이 아니었음.
>- 오늘도 이거 다 쓰고 웹사이트 올리러 갑니다.
>- 아아아아아악 만드는 것보다 게시가 훨씬 어려움!!!
>- 그래서 웹사이트 게시하는 방법은 따로 안 올립니다.
>- 그것까지 하나하나 설명할 정신이 없음.
>- 필요하면 그냥 따로 가서 알려드림;

>[!error] Error
>- 솔직히 하루만에 다음 과제 저걸 다 할 수 있을지 모르겠음.
>- DB 건드려야됨 물론 깊게 건드는건 아니지만.
>- 왜 Error냐면 제가 이 콜아웃을 써보고 싶었기 때문입니다.
>- 그리고 일하기가 싫어요.
>- 근데 제가 벌인 일이죠.
>- 지금 새벽 4시임.
